sudo: required
services:
  - docker

before_install:
  - docker build -t prayushbogati/docker-test-app -f Dockerfile.dev .

script:
  - docker run -e CI=true prayushbogati/docker-test-app npm run test